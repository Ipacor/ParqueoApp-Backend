# ========================
# Puerto del servidor
# ========================
server.address=0.0.0.0
server.port=8080

# ========================
# Configuraciones de seguridad del servidor
# ========================
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=strict
server.servlet.session.timeout=30m

# ========================
# Configuración de la base de datos
# ========================
spring.datasource.url=jdbc:mysql://metro.proxy.rlwy.net:58449/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=egbGsKxUdhldNpkzbNQEudnLFCGXEiHc
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================
# JPA y Hibernate
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ========================
# Configuraciones de Jackson para seguridad
# ========================
spring.jackson.serialization.fail-on-self-references=false
spring.jackson.deserialization.fail-on-unknown-properties=true

# ========================
# Configuraciones de seguridad de Spring Security
# ========================
#spring.security.user.name=admin
#spring.security.user.password=admin123

# ========================
# Configuraciones de logging
# ========================
logging.level.org.springframework.security=DEBUG
logging.level.com.parqueo.parkingApp.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.springframework.orm.jpa=DEBUG
logging.level.org.springframework.transaction=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.root=DEBUG

# ========================
# Configuraciones de Swagger/OpenAPI
# ========================
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# ========================
# Configuraciones de validación
# ========================
spring.validation.enabled=true

# ========================
# Configuración extra (desactiva recursos estáticos automáticos)
# ========================
spring.web.resources.add-mappings=false

